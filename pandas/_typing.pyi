import datetime
import numpy as np
import sys
from pandas import Interval as Interval
from pandas._libs import Period as Period, Timedelta as Timedelta, Timestamp as Timestamp
from pandas.core.arrays.base import ExtensionArray as ExtensionArray
from pandas.core.dtypes.dtypes import ExtensionDtype as ExtensionDtype
from pandas.core.frame import DataFrame as DataFrame
from pandas.core.generic import NDFrame as NDFrame
from pandas.core.indexes.base import Index as Index
from pandas.core.series import Series as Series
from pathlib import Path
from typing import Any, AnyStr, Callable, Collection, Dict, Hashable, IO, List, Mapping, NewType, Optional, Sequence, TypeVar, Union
if sys.version_info >= (3, 8):
    from typing import Literal, Protocol
else:
    from typing_extensions import Literal, Protocol

AnyArrayLike = TypeVar('AnyArrayLike', 'ExtensionArray', 'Index', 'Series', np.ndarray)
ArrayLike = TypeVar('ArrayLike', 'ExtensionArray', np.ndarray)
PythonScalar = Union[str, int, float, bool]
DatetimeLikeScalar = TypeVar('DatetimeLikeScalar', 'Period', 'Timestamp', 'Timedelta')
PandasScalar: Any
Scalar = Union[PythonScalar, PandasScalar]
Dtype: Any
FilePathOrBuffer = Union[str, Path, IO[AnyStr]]
FrameOrSeriesUnion: Any
FrameOrSeries = TypeVar('FrameOrSeries', bound='NDFrame')
Axis = Union[str, int]
Label = Optional[Hashable]
Level = Union[Label, int]
Ordered = Optional[bool]
JSONSerializable = Union[PythonScalar, List, Dict]
Axes = Collection
Renamer = Union[Mapping[Label, Any], Callable[[Label], Label]]
T = TypeVar('T')

# Abover were generated by stubgen; pylance ones are below

_str = str  # needed because Series/DataFrame have properties called "str"...
_bool = bool  # ditto
num = Union[int, float]
_SeriesAxisType = Literal["index", 0]  # Restricted subset of _AxisType for series
_AxisType = Literal["columns", "index", 0, 1]
_DType = TypeVar("_DType", bool, int, float, object)
_DTypeNp = TypeVar("_DTypeNp", bound=np.dtype)
_KeysArgType = Any
_ListLike = TypeVar("_ListLike", Sequence, np.ndarray, Series)
_StrLike = Union[str, np.str_]
_Path_or_Buf = FilePathOrBuffer
_LevelType = Union[int, str]
_Scalar = Union[str, bytes, datetime.date, datetime.datetime, datetime.timedelta, bool, int, float, complex]
_IndexType = Union[slice, np.ndarray, Index[int], List[int], Series[int]]
_MaskType = Union[Series[bool], np.ndarray, List[bool]]
# Refine the next 3 in 3.9 to use the specialized type.
_np_ndarray_int64 = NewType("_np_ndarray_int64", np.ndarray)
_np_ndarray_bool = NewType("_np_ndarray_bool", np.ndarray)
_np_ndarray_str = NewType("_np_ndarray_str", np.ndarray)
_T = TypeVar("_T", str, int)
_U = TypeVar("_U", str, int)
