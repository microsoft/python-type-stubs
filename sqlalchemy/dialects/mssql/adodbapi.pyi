from sqlalchemy import util as util
from sqlalchemy.dialects.mssql.base import MSDateTime as MSDateTime, MSDialect as MSDialect
from typing import Any

class MSDateTime_adodbapi(MSDateTime):
    def result_processor(self, dialect: Any, coltype: Any): ...

class MSDialect_adodbapi(MSDialect):
    supports_sane_rowcount: bool = ...
    supports_sane_multi_rowcount: bool = ...
    supports_unicode: Any = ...
    supports_unicode_statements: bool = ...
    driver: str = ...
    @classmethod
    def import_dbapi(cls): ...
    colspecs: Any = ...
    def create_connect_args(self, url: Any): ...
    def is_disconnect(self, e: Any, connection: Any, cursor: Any): ...
dialect = MSDialect_adodbapi
