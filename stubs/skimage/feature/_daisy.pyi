def daisy(
    image,
    step: int = 4,
    radius: int = 15,
    rings: int = 3,
    histograms: int = 8,
    orientations: int = 8,
    normalization="l1",
    sigmas=None,
    ring_radii=None,
    visualize: bool = False,
): ...
