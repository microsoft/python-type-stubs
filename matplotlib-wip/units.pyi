import datetime
import numpy as np
from typing import Any, Callable, Iterable, Literal, Sequence
from matplotlib._typing import *
from matplotlib.ticker import Locator
from matplotlib.ticker import Formatter
from matplotlib.units import AxisInfo
from matplotlib.axis import Axis

"""
This type stub file was generated by pyright.
"""
from datetime import date
from matplotlib.category import StrCategoryConverter
from matplotlib.dates import ConciseDateConverter, _SwitchableDateConverter
from typing import Any, Optional, Tuple, Union

from . import _api

"""
This type stub file was generated by pyright.
"""

class ConversionError(TypeError): ...

class AxisInfo:
    """
    Information to support default axis labeling, tick labeling, and limits.

    An instance of this class must be returned by
    `ConversionInterface.axisinfo`.
    """

    def __init__(
        self,
        majloc: Any = ...,
        minloc: None = ...,
        majfmt: Any = ...,
        minfmt: None = ...,
        label: Optional[str] = ...,
        default_limits: Optional[Tuple[date, date]] = ...,
    ) -> None:
        """
        Parameters
        ----------
        majloc, minloc : Locator, optional
            Tick locators for the major and minor ticks.
        majfmt, minfmt : Formatter, optional
            Tick formatters for the major and minor ticks.
        label : str, optional
            The default axis label.
        default_limits : optional
            The default min and max limits of the axis if no data has
            been plotted.

        Notes
        -----
        If any of the above are ``None``, the axis will simply use the
        default value.
        """
        ...

class ConversionInterface:
    """
    The minimal interface for a converter to take custom data types (or
    sequences) and convert them to values Matplotlib can use.
    """

    @staticmethod
    def axisinfo(unit, axis):
        """Return an `.AxisInfo` for the axis with the specified units."""
        ...
    @staticmethod
    def default_units(x, axis):
        """Return the default unit for *x* or ``None`` for the given axis."""
        ...
    @staticmethod
    def convert(obj, unit, axis):
        """
        Convert *obj* using *unit* for the specified *axis*.

        If *obj* is a sequence, return the converted sequence.  The output must
        be a sequence of scalars that can be used by the numpy array layer.
        """
        ...
    @staticmethod
    def is_numlike(x: str) -> bool:
        """
        The Matplotlib datalim, autoscaling, locators etc work with scalars
        which are the units converted to floats given the current unit.  The
        converter may be passed these floats, or arrays of them, even when
        units are set.
        """
        ...

class DecimalConverter(ConversionInterface):
    """Converter for decimal.Decimal data to float."""

    @staticmethod
    def convert(value: Decimal | Iterable, unit, axis):
        """
        Convert Decimals to floats.

        The *unit* and *axis* arguments are not used.

        Parameters
        ----------
        value : decimal.Decimal or iterable
            Decimal or list of Decimal need to be converted
        """
        ...

class Registry(dict):
    """Register types with conversion interface."""

    def get_converter(
        self, x: Any
    ) -> Optional[
        Union[ConciseDateConverter, StrCategoryConverter, _SwitchableDateConverter]
    ]:
        """Get the converter interface instance for *x*, or None."""
        ...

registry: dict = ...
